package com.awk.datastore;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeResourceControllerIT extends ResourceControllerTest {

    // Execute the same tests but in native mode.
}